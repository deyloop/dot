#!/usr/bin/bash

# Enable shell integration after installing OpenCode.
# https://opencode.ai

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=../bash/profile-link.sh
source "$SCRIPT_DIR/../bash/profile-link.sh"
link_bash_profile opencode
